import { FeatureSection } from '@/widgets';
import React from 'react';

const Features = () => {
  return (
    <section className='flex-col flexCenter overflow-hidden bg-feature-bg bg-center bg-no-repeat py-24'>
      <FeatureSection />
    </section>
  );
};

export default Features;
